server:
  port: 8083

spring:
  config:
    import: "optional:configserver:http://config-server:8888"
  application:
    name: inventory-service
  data:
    mongodb:
      uri: mongodb://inventory-mongo:27017/inventorydb

# other settings (Zipkin, Eureka, management) are supplied by the central config server

